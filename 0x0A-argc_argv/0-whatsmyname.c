#include <stdlib.h>
#include <stdio.h>

/**
*main - check main
*@argc: number of arquments
*@argv: an array of strings
*Return: Always 0
*/

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

