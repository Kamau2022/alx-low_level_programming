#include "main.h"
#include <string.h>


/**
*_strlen_recursion - a function that returns length of a string
*@s: an input string
*Return: an integer
*/

int _strlen_recursion(char *s)
{
	
	int l;
	
	l = strlen(s);
	return (l);
}
