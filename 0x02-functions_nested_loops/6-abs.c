#include "main.h"

/**
*_abs - a function that computes absolute
*value of an integer
*@x: an input integer
*Return: an integer
*/

int _abs(int x)
{

	int k = 0;

	if (x >= k)
	{
	return (x);
	}
	else
	{
	return (x * -1);
	}
}
