#include "main.h"
#include <string.h> 
/**
*_strlen - a function that gives the lenght of a string
*@s - a character variable
*Return: length of a string
*/

int _strlen(char *s)
{
	int length;
	length = strlen(s);
	return (length);
}
